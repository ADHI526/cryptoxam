
#include <stdio.h>
#include <math.h>
int main() 
{
    int i;
    double log2_factorial = 0.0;
    for (i = 1; i <= 25; i++) 
	{
        log2_factorial += log2(i);
    }
    printf("The number of possible Playfair cipher keys is approximately 2^%.2f\n", log2_factorial);

    return 0;
}
